#include <iostream>

#include <Fixed.h>

int main(int agrc, char* argv[])
{
    /**
     * Ex01
     */
    
    // Fixed a(1);
    // Fixed b(a);
    // Fixed c(std::move(b));
    
    // std::cout << a.getRawBits() << "\n";
    // std::cout << b.getRawBits() << "\n";
    // std::cout << c.getRawBits() << "\n";

    /**
     * Ex02
     */

    /**
     * Ex03
     */
    Fixed a();

    
    return 0;
}